function func() {
	i = 0;
	obj = { a: 1 }
	
	if (i == 0);
	
	while (i != 0);

	do ; while (i != 0);
	
	for (i = 0; i < 0; i++);
	
	for (i in new Array());
	
	with(obj);
	
	if (i == 0) {}
	
	switch(i) {}
	
	while (i != 0) {}

	do {} while (i != 0);
	
	for (i = 0; i < 0; i++) {}
	
	for (i in new Array()) {}
	
	with(obj) {}

	return 0;
}
